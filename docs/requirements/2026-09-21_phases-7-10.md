# Requirement — Phases 7, 8, 9, 10 in one go (2026-09-21, evening)

## Owner's words (verbatim, Hinglish)

> Phase 7 , 8 , 9 , 10 poora step by step complete kar ke update do , aur sab complete ho jaye to jo
> requirement hogi batana mai kar dunga to code ready rkho , aur exness raw ka 10 usd maan ke chalo 1 lot ka

With a screenshot of the remaining phases table (7 Structure & Patterns, 8 Research Engine,
9 Raw account costs, 10 ML + AI Assistant, 11 Paper Trading, 12 20-year data, 13 Research Gate).

## Decisions recorded

| Topic | Decision |
|---|---|
| Scope | Build Phases 7–10 completely; where a phase needs something only the owner can provide, keep the code ready and list the requirement at the end. |
| Raw commission (A4) | **USD 10 per lot, round turn** on the Exness Raw Spread account, as stated by the owner. Used by the Raw cost profile in every cost scenario (a stated account term, not a guess). If the real figure turns out to be per side, rebuild with `--commission-per-lot 20` — the profile keeps the value it was built with. |
| Raw spreads before A7 | Unknown until a Raw Spread demo's ticks exist. The **provisional** Raw profile uses the demo account's spread tables as an upper bound (a Raw spread is the demo spread minus the markup). It never promotes: §15 promotion waits for the Raw profile calibrated from Raw-account ticks (research note 3). |
| AI Assistant API mode | Uses the official `anthropic` SDK against `CI_LLM_BASE_URL` (AgentRouter), model `claude-opus-5`. On 2026-09-21 the router answered **402: budget pool quota exhausted** — the key and connection work; the owner needs to top up the AgentRouter budget. |
